import { ClearDateIcon } from "@/assets";

export const ClearDate: React.FC = () => {
  return (
    <div className="absolute right-0 top-0 flex items-center justify-center size-4 rounded-full bg-secondary">
      <ClearDateIcon />
    </div>
  );
};
